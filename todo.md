# Project TODO

- [x] 设计数据表：PCAP 数据集、流特征、模型版本与检测任务。
- [x] 实现安全的 PCAP 上传、文件元数据保存与对象存储引用。
- [x] 实现离线 PCAP 解析：五元组、双向流统计、SPLT、TLS/QUIC 可见元数据与协议分布。
- [x] 实现数据集标签管理、特征预览和删除功能。
- [x] 实现可选择特征集和算法的训练流程，并保存准确率、精确率、召回率与 F1。
- [x] 实现模型版本持久化、历史查看和指定模型加载为当前模型。
- [x] 实现检测 PCAP 上传、模型选择、逐流风险评分、高风险详情与统计摘要。
- [x] 构建专业、响应式仪表盘及上传、数据集、训练、模型、检测页面。
- [x] 编写 Vitest 单元测试，覆盖 PCAP 特征提取、模型训练和风险评分核心逻辑。
- [x] 验证核心端到端流程、截图审查并保存最终项目版本。
- [x] 在仪表盘展示 PCAP、模型、检测任务概览、最新结果摘要和核心功能入口。
- [x] 在上传流程展示拖拽/点击上传、解析进度、文件大小、包数与协议分布。
- [x] 在特征提取中覆盖双向流统计、SPLT、TLS/QUIC 可见元数据与 SNI 可见性。
- [x] 在数据集页面支持良性/恶意/未标注标签、特征预览和安全删除。
- [x] 在训练页面支持数据集选择、特征集/算法配置、阶段进度与 Accuracy、Precision、Recall、F1 展示。
- [x] 在模型页面支持训练元数据、评估指标、历史版本查看与激活当前检测模型。
- [x] 在检测页面支持目标 PCAP、模型选择、逐流风险评分、高风险五元组/特征/原因和整体统计。
- [x] 将上传页进度改为基于实际文件读取进度与真实服务端解析阶段的状态展示。
- [x] 为 QUIC 增加长包头版本识别，避免仅依据 UDP 端口启发式标记协议。
- [x] 将训练页改为轮询真实训练任务状态和 progress，移除前端定时模拟进度。
- [x] 在检测结果中展示高风险流的关键特征值。
- [x] 为 PCAP 上传/解析增加后端任务状态与进度轮询，展示 queued、processing、completed、failed 的真实状态。
- [x] 使用最小可用 PCAP 跑通上传、解析、数据集、训练、模型激活与检测分析全链路。
- [x] 补充覆盖上传任务、训练任务和检测任务的集成测试。
- [x] 保存包含端到端验证脚本与最终回归结果的最终项目版本。
- [x] 将侧栏、页面标题、按钮和工作流中的模块名统一为四字结构。
- [x] 将数据集标签、训练引擎、模型参数和检测结果升级为多分类体系。
- [x] 在训练页面支持选择不少于两种恶意类别并展示多分类评估指标。
- [x] 在检测页面展示逐流预测类别、类别置信度和各类别风险分布。
- [x] 新增独立 HTTP PCAP 检测接口，支持 multipart 文件上传、模型版本选择与 JSON 结果返回。
- [x] 编写多分类训练和 HTTP 检测接口测试，并完成最终回归验证。
- [x] 全面统一首页快捷入口、按钮、空状态和工作流文案中的模块名为四字结构。
- [x] 定义并持久化逐流详细检测结果：五元组、双向流统计、SPLT 摘要、TLS/QUIC 可见元数据及多分类结果。
- [x] 扩展 HTTP 检测接口 JSON，完整返回任务级统计、逐流详细信息、类别概率和风险解释。
- [x] 升级流量检测页面，支持展开查看详细流信息与所有类别置信度。
- [x] 增加详细结果端到端断言并完成回归验证。
- [x] 验证持久化检测详情包含五元组、TLS/QUIC、SPLT 和类别概率等详细字段。
- [x] 在详细结果改动后完成生产构建并保存最终版本。
- [x] 保存包含详细检测结果持久化、HTTP JSON 和详情页的最终检查点。
- [x] 准备并上传三类可控 PCAP 样本，验证页面上传、解析进度和数据集创建。
- [x] 通过数据管理页面完成多分类标注，并在模型训练页面训练、保存和激活模型。
- [x] 通过流量检测页面上传待测 PCAP，验证分类分布、详情弹窗和接口密钥生成。
- [x] 检查浏览器控制台与网络请求，修复 Web 界面测试发现的问题并复验。
- [x] 从沙箱浏览器环境执行完整页面交互测试，并记录登录态或权限带来的可验证边界。
- [x] 在已发布域名执行无登录访问、模型加载、流量检测页和接口服务入口的页面验收。
- [x] 移除前端登录门槛并将仪表盘、样本上传、数据管理、模型训练、模型版本和流量检测设为公开访问。
- [x] 将业务数据统一归属到受控公共工作区，移除 tRPC 路由对用户会话的强制依赖。
- [x] 保留独立 HTTP 检测接口的 API 密钥校验，并验证公开界面和接口的安全边界。
- [x] 在已发布域名复验无登录访问、PCAP 上传、多分类训练和详细检测结果流程。
- [x] 在数据管理页面实际修改至少一个数据集类别并验证标签持久化，再完成训练回归。
- [x] 定位流量检测页面 ResizeObserver 通知循环的尺寸观测与布局更新触发点。
- [x] 修复检测页及共享布局中的 ResizeObserver 循环，避免控制台持续报错。
- [x] 在桌面和移动尺寸下复验检测详情与侧栏布局，确认无新的观察器错误。
- [x] 在已发布域名生成接口密钥，并验证页面提示和密钥状态刷新。
- [x] 在已发布域名上传 PCAP 并完成一次检测，验证任务执行与结果渲染。
- [x] 在已发布域名打开至少一条逐流详情，验证五元组、TLS/QUIC、SPLT、多分类概率和风险解释展示。
- [x] 持久化并展示训练数据集、模型版本和检测任务的完整历史记录及详情。
- [x] 新增操作审计日志，记录上传、标签变更、训练、模型激活、检测和接口密钥操作。
- [x] 新增历史中心页面，支持按类型查看、检索和分页浏览历史数据。
- [x] 支持将训练数据、模型信息、检测结果和操作日志下载为 CSV 或 JSON 文件。
- [x] 编写历史持久化、审计和导出测试，并完成浏览器端下载验证。
- [x] 为训练数据、模型版本与检测任务实现可展开的历史详情，并在界面中验证详情内容。
- [x] 为数据集、模型、检测任务与审计日志实现后端分页查询和前端翻页控件。
- [x] 在浏览器中实际下载模型信息和检测结果的 CSV/JSON 文件，补齐导出验收证据。
- [x] 在历史归档页实际打开模型版本详情，验证版本号、算法、类别集合和指标字段展示。
- [x] 核验目标 GitHub 仓库状态及当前项目待提交文件。
- [x] 编写项目部署说明、运行手册和技术架构文档。
- [x] 将代码、数据库迁移、测试、部署与架构文档提交到指定 GitHub 仓库。
- [x] 核验远程仓库提交内容与默认分支状态。
- [x] 核验远程主分支包含 README、docs、drizzle、server、client、scripts 和测试等关键路径。
- [x] 新增可配置标注集，支持名称、描述、类别映射、启停与默认设置。
- [x] 在训练流程中选择标注集，并将标签定义快照持久化到模型版本。
- [x] 在检测流程中引用模型关联的标注集，并将标签定义快照持久化到检测任务和详细结果。
- [x] 新增标注配置页面，支持创建、编辑、删除、启停和默认设置操作。
- [x] 编写标注集持久化、训练检测引用和页面交互测试；不得引入标准集实体或界面。
- [x] 已取消：不新增标准集；系统仅保留可配置标注集。
- [x] 已取消：标注集不关联标准集，直接维护类别映射、启停和默认设置。
- [x] 已取消：训练流程仅选择标注集，并将其配置快照持久化到模型版本。
- [x] 已取消：检测流程不选择标准集，使用模型关联标注集快照。
- [x] 已完成替代项：新增标注配置页面，支持创建、编辑、删除、启停和默认设置操作。
- [x] 已完成替代项：标注集持久化、训练检测引用和页面交互测试均已覆盖。
- [x] 为标注配置页面补充标注集级启用/停用控件，并持久化到 annotationSets.isActive。
- [x] 补充标注集启停的端到端与浏览器验证，确认停用集不再可用于上传和训练选择。
- [x] 在浏览器中停用非默认标注集后，验证其从样本上传和模型训练选择器消失；重新启用后验证恢复可选。
- [x] 在浏览器中重新启用非默认标注集后，打开模型训练页标注集选择器，确认其恢复可选。
- [ ] 评估并确定与当前 Node 托管环境兼容的成熟开源机器学习框架。
- [x] 采用开源框架实现多分类训练、概率预测、模型序列化和加载。
- [x] 保持标注集快照、模型版本、检测详情、历史归档和 HTTP 检测接口兼容。
- [x] 将训练算法配置改为开源框架支持的成熟分类器并更新界面说明。
- [x] 编写开源框架训练、推理、模型加载和 API 检测回归测试。
- [x] 复核《面向 TLS1.3/ECH 的机器学习加密流量检测：开源实现方案》并明确采用的开源组件。
- [ ] 按方案集成 NFStream 流特征与 TLS/JA3/SPLT 元数据提取能力，保留现有 PCAP 详情输出。
- [x] 按方案集成 Kitsune/KitNET 无监督异常检测，并与监督多分类结果联合展示。
- [x] 使用方案推荐的开源机器学习实现替代当前临时 TensorFlow.js 改造，保留模型版本和 HTTP 接口。
- [ ] 用公开或用户提供的真实 PCAP 完成 TLS1.3/ECH 无解密检测验证，并记录可见性边界。
- [x] 评估并确定与当前 Node 托管环境兼容的成熟开源机器学习框架。
- [x] 采用开源 LightGBM 和 Kitsune KitNET 实现多分类训练、概率预测、模型序列化和加载。
- [x] 保持标注集快照、模型版本、检测详情、历史归档和 HTTP 检测接口兼容。
- [x] 将训练算法配置改为开源 LightGBM 与 Kitsune KitNET 融合分类器并更新界面说明。
- [x] 编写开源框架训练、推理、模型加载和 API 检测回归测试。
- [ ] 将现有 PCAP 解析器替换或并联为 NFStream 特征提取适配器，并与既有 TLS/QUIC 详情字段做交叉验证。
- [ ] 检索并筛选公开可复现的 TLS 1.3、QUIC 与加密恶意流量样本，审查其来源与许可。
- [ ] 下载最小必要公开 PCAP 样本并以 NFStream 提取流统计、SPLT 与可见 TLS/QUIC 元数据。
- [ ] 下载并验证至少一个公开 TLS 1.3 PCAP 与一个公开 QUIC PCAP，分别记录来源、许可和可复现获取方式。
- [ ] 用 NFStream 对 TLS 1.3 与 QUIC 样本输出并保存流统计、SPLT 和可见 TLS/QUIC 元数据结果。
- [ ] 筛选一份公开加密恶意流量样本或带恶意标签的数据源，明确许可及其是否适合无解密验证。
- [ ] 使用公开真实样本验证无解密 LightGBM 与 KitNET 检测链路，并记录 TLS 1.3/ECH 可见性边界。
- [ ] 将样本来源、许可、命令和验证结果写入项目文档。
- [ ] 使用已完整下载的 TLS 1.3 与 QUIC PCAP 完成 NFStream 包级特征验证，不下载任何恶意二进制。
- [ ] 获取 MCFP 的公开标签或流记录，完成 LightGBM 与 KitNET 监督/异常检测验证。
- [ ] 将 MCFP 标签流记录验证与 PCAP 包级验证分开记录，避免将二者混作同一数据源结论。
- [x] 生成安全合成 TLS 握手 PCAP，包含 ClientHello、TLS 版本、密码套件、扩展、SNI 和 JA3 所需字段。
- [x] 验证现有解析器从合成 PCAP 提取五元组、TLS 版本、JA3、SNI 可见性、SPLT 和流统计。
- [ ] 使用多个安全合成 TLS 流跑通标注、LightGBM/KitNET 训练、模型保存、加载和 HTTP 检测。
- [ ] 将合成 TLS PCAP 的字段覆盖和非真实性能结论边界写入验证文档。
- [ ] 生成不含恶意代码或攻击载荷的合成 PCAP，覆盖正常、命令控制和数据外传标签的流统计差异。
- [ ] 使用合成 PCAP 跑通上传、特征提取、标注、LightGBM/KitNET 训练、模型保存和检测接口。
- [ ] 记录合成测试只验证系统处理链路，不代表真实 TLS1.3/ECH 或恶意检测性能。
