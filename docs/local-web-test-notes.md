# 本地 Web 界面验收记录

## 2026-08-13：首页

本地开发服务以 `http://localhost:3000/` 启动。浏览器确认 React 页面已完成渲染，控制台未出现运行时错误。首页正确展示公共工作区、八个功能模块导航、已上传 PCAP、模型版本与检测任务统计卡片，以及样本上传、模型训练、流量检测工作流引导。

初始数据库未配置，因此首页统计均为 0；这不影响页面布局、导航和前端渲染验收。后续应使用具备 MariaDB/MinIO 的部署环境验证上传、训练、模型激活和检测的持久化闭环。

## 样本上传页面

导航与页面路由正常。页面显示标注集选择器、初始类别选择器、拖放区域、PCAP 文件选择按钮、20 MB 文件大小限制以及包数/流数/协议分布结果区域；页面视觉布局和交互控件均正常渲染。本地环境缺少 MariaDB/MinIO 和 Forge 对象存储配置，因而未继续提交文件，以避免把预计失败的持久化请求误判为界面缺陷。

## 数据管理页面

页面正确显示 PCAP 文件、流/包、标注集、流量类别、上传时间和操作列，并在数据查询完成后展示“尚未上传数据集”空状态，没有前端异常。NFStream 并联特征说明已在页面副标题中呈现；逐流明细需在有真实数据集的部署环境中继续验证。

## 标注配置页面

页面路由、说明文本与“新建标注集”操作入口正常渲染。由于本地未接入 MariaDB，默认标注集初始化请求无法完成，页面保持稳定的初始化状态而未产生浏览器错误。五类标签的创建、编辑、启停与设为默认需要在具备数据库的发布环境中继续执行交互验收。

## 模型训练页面

页面完整展示 Abonnen/Malicious_TLS_Detection 核心引擎说明、固定 30 项 Zeek TLS/证书/DNS 特征，以及五类概率输出语义。算法选择器经交互验证可在“随机森林（上游参数）”与“GBDT（上游参数）”之间切换，训练执行卡片文案同步更新。当前因未连接数据库且无训练数据集，未触发实际训练任务。

## 模型版本页面

模型版本管理页面正确显示版本参数、特征集、训练数据引用和评估指标的用途说明；在查询完成后提供“暂无可用模型版本”的稳定空状态。已验证此前加入的历史模型只读标识逻辑可通过代码与构建检查，实际模型激活按钮和指标卡片将随发布环境训练出的版本继续复验。

## 流量检测页面

页面正确展示模型版本选择器、无可用模型的引导提示，以及独立检测接口契约：`POST /api/v1/detect`、multipart `file` 字段、可选 `modelVersionId` 和 `Authorization: Bearer API_KEY`。API Key 生成入口可见且已明确仅保存密钥前缀；由于本地数据库未配置，未创建真实密钥或启动检测任务。

## 历史归档页面

页面显示训练数据、模型版本、检测任务和审计日志四个汇总卡片，提供全文检索、四类归档标签页与分页控件。已交互切换至“模型版本”页，表头正确切换为算法/类别与状态字段，空状态稳定渲染。实际 CSV/JSON 导出、详情及分页数据需在已发布的持久化环境中继续验证。

## 接口与前端运行时检查

本地服务的 `system.health` tRPC 端点返回 `ok: true`；`GET /api/v1/detect/health` 返回 HTTP 200 和包含 `engine: "Abonnen/Malicious_TLS_Detection"` 的 JSON 响应。完成所有页面导航后，浏览器控制台仅存在 React DevTools 提示，未发现前端运行时错误。

## 外部预览验证

已通过临时公开预览地址重新加载首页。八个功能模块导航、首页统计卡片、检测任务空状态和工作流引导均能从外部代理访问。该预览依赖当前本地服务会话，不替代目标服务器的持久 Docker Compose 部署；目标服务器的 SSH 服务恢复后仍应按照 `docs/DEPLOYMENT.md` 执行完整部署、迁移和端到端验收。
